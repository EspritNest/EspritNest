{% extends 'front/index.html.twig' %}

{% block title %}Edit AnnoncesColocation{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Edit AnnoncesColocation</h1>

<div class="card shadow-sm p-4">

    {{ include('annonces/_form.html.twig', {'button_label': 'Update'}) }}
    
</div>
    <div class="mt-3 text-center">
    <a href="{{ path('app_annonces_index') }}" class="btn btn-secondary">
    <i class="bi bi-arrow-left"></i>back to list</a>
</div>

    {{ include('annonces/_delete_form.html.twig') }}
    
    </div>


    <style>
        .btn i {
            font-size: 1.2rem;
            margin-right: 5px;
        }
        .card {
            border-radius: 10px;
            background-color: #f9f9f9;
        }
    </style> 
{% endblock %}
