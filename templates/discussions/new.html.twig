{% extends 'front/index.html.twig' %}

{% block body %}
    <h1>Create new Discussion</h1>

    {{ form_start(form) }}
        {{ form_row(form.participant2) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_discussions_index') }}">back to list</a>
{% endblock %}
